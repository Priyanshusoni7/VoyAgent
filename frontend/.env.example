# Copy to frontend/.env.local
#
# MANDATORY
# Base URL of the Spring Boot backend, including the /api prefix.
# Used by frontend/src/lib/axios.js. Must be reachable from the BROWSER,
# not from the server, because every call is made client-side.
NEXT_PUBLIC_API_URL=http://localhost:5000/api
